const mutltiplication = (a: number, b: number, c: number) : number => {

    return a * b * c
}

let x = 4

let y = 11

let z = 3.4

console.log(`${x} * ${y} * ${z} = ${mutltiplication(x, y, z)}`) // 4 * 11 * 3.4 = 149.6
